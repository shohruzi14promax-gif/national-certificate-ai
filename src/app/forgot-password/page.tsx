'use client';
import Link from 'next/link'; import {FormEvent,useState} from 'react'; import {createClient} from '@/lib/supabase';
export default function Forgot(){const supabase=createClient();const [email,setEmail]=useState('');const [message,setMessage]=useState('');const [error,setError]=useState('');async function submit(e:FormEvent){e.preventDefault();setMessage('');setError('');const {error}=await supabase.auth.resetPasswordForEmail(email,{redirectTo:`${window.location.origin}/reset-password`});if(error)setError(error.message);else setMessage('Agar email mavjud bo‘lsa, parolni tiklash havolasi yuborildi.');}return <main className="auth-page"><div className="auth-card"><Link href="/login" className="brand">← Kirishga qaytish</Link><div className="kicker">PAROLNI TIKLASH</div><h1>Parolni unutdingizmi?</h1><p>Email manzilingizni kiriting.</p><form onSubmit={submit} className="form-card"><label>Email<input type="email" value={email} onChange={e=>setEmail(e.target.value)} required /></label>{error&&<div className="error-box">{error}</div>}{message&&<div className="success-box">{message}</div>}<button className="primary">Tiklash havolasini yuborish</button></form></div></main>}
