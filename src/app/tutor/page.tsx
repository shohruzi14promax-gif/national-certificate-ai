'use client';
import {FormEvent,useState} from 'react'; import Link from 'next/link';
export default function Tutor(){const [message,setMessage]=useState(''),[reply,setReply]=useState(''),[conversationId,setConversationId]=useState<string>(),[loading,setLoading]=useState(false),[error,setError]=useState('');async function send(e:FormEvent){e.preventDefault();if(!message.trim())return;setLoading(true);setError('');const r=await fetch('/api/tutor',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({message,conversationId})});const j=await r.json();if(!r.ok)setError(j.error);else{setConversationId(j.conversationId);setReply(j.reply);setMessage('')}setLoading(false)}return <main className="tutor-page"><header className="dash-nav"><Link href="/dashboard" className="brand"><span className="brand-mark">N</span> National Certificate AI</Link><Link href="/practice">Practice</Link></header><section className="tutor-wrap"><div className="kicker">TUTOR</div><h1>Savolingizni tushuntirib beramiz.</h1><p className="lead">Mavzuni tushuntirish, hint, xato javobni tahlil qilish yoki misol so‘rashingiz mumkin.</p><div className="chat-card">{reply&&<div className="assistant-bubble">{reply}</div>}{error&&<div className="error-box">{error}</div>}<form onSubmit={send}><textarea value={message} onChange={e=>setMessage(e.target.value)} placeholder="Masalan: Kvadrat tenglamani sodda qilib tushuntir…" rows={5} /><button className="primary" disabled={loading}>{loading?'Javob tayyorlanmoqda…':'Savol yuborish →'}</button></form></div></section></main>}
