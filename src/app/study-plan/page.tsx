'use client';
import {FormEvent,useState} from 'react'; import Link from 'next/link';
export default function StudyPlan(){const [date,setDate]=useState(''),[level,setLevel]=useState('Boshlang‘ich'),[minutes,setMinutes]=useState(60),[target,setTarget]=useState(70),[plan,setPlan]=useState<any>(null),[error,setError]=useState(''),[loading,setLoading]=useState(false);async function submit(e:FormEvent){e.preventDefault();setLoading(true);setError('');const r=await fetch('/api/study-plan',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({examDate:date,targetScore:target,dailyMinutes:minutes,currentLevel:level})});const j=await r.json();if(!r.ok)setError(j.error);else setPlan(j);setLoading(false)}return <main className="plan-page"><header className="dash-nav"><Link href="/dashboard" className="brand"><span className="brand-mark">N</span> National Certificate AI</Link><Link href="/practice">Practice</Link></header><section className="plan-wrap"><div className="kicker">PERSONAL STUDY PLAN</div><h1>Imtihoningizga mos reja.</h1><p className="lead">Reja real natijalaringizdagi zaif mavzularni ustuvor qiladi va vaqt yaqinlashganda mocklarni ko‘paytiradi.</p><form onSubmit={submit} className="form-card"><label>Imtihon sanasi<input type="date" value={date} onChange={e=>setDate(e.target.value)} required /></label><label>Maqsad ball<select value={target} onChange={e=>setTarget(Number(e.target.value))}>{[60,70,80,90,95].map(x=><option key={x} value={x}>{x}</option>)}</select></label><label>Hozirgi daraja<select value={level} onChange={e=>setLevel(e.target.value)}><option>Boshlang‘ich</option><option>O‘rta</option><option>Yaxshi</option><option>Yuqori</option></select></label><label>Kunlik vaqt<select value={minutes} onChange={e=>setMinutes(Number(e.target.value))}><option value="30">30 daqiqa</option><option value="60">1 soat</option><option value="90">1.5 soat</option><option value="120">2 soat</option><option value="180">3 soat</option></select></label>{error&&<div className="error-box">{error}</div>}<button className="primary" disabled={loading||!date}>{loading?'Reja tuzilmoqda…':'Rejani yaratish →'}</button></form>{plan&&<div className="generated"><div><b>{plan.days}</b><span>kunlik yo‘l xaritasi</span></div><div><b>{minutes} daq.</b><span>kunlik vaqt</span></div><div><b>{target}</b><span>maqsad ball</span></div><div className="weekly"><h2>Reja saqlandi</h2><p>Bugundan boshlab mavzular natijangizga qarab ustuvorlashtiriladi. Har bir testdan keyin keyingi reja qayta hisoblanishi mumkin.</p><Link href="/dashboard" className="secondary">Dashboardga o‘tish</Link></div></div>}</section></main>}
