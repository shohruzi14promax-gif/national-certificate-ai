'use client';
import {FormEvent,useEffect,useState} from 'react'; import Link from 'next/link'; import {createClient} from '@/lib/supabase';
export default function StudyPlan(){const supabase=createClient();const [subjects,setSubjects]=useState<any[]>([]),[selected,setSelected]=useState<string[]>([]),[date,setDate]=useState(''),[level,setLevel]=useState('Boshlang‘ich'),[minutes,setMinutes]=useState(60),[target,setTarget]=useState(70),[plan,setPlan]=useState<any>(null),[error,setError]=useState(''),[loading,setLoading]=useState(true);useEffect(()=>{supabase.from('subjects').select('id,name,icon').order('sort_order').then(({data})=>{const s=data??[];setSubjects(s);setSelected(s.map((x:any)=>x.id)) ;setLoading(false)})},[supabase]);async function submit(e:FormEvent){e.preventDefault();if(!selected.length){setError('Kamida bitta fan tanlang.');return}setLoading(true);setError('');const r=await fetch('/api/study-plan',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({examDate:date,targetScore:target,dailyMinutes:minutes,currentLevel:level,selectedSubjects:selected})});const j=await r.json();if(!r.ok)setError(j.error||'Reja yaratilmadi');else setPlan(j);setLoading(false)}return <main className="plan-page"><header className="dash-nav"><Link href="/dashboard" className="brand"><span className="brand-mark">N</span> National Certificate AI</Link><Link href="/practice">Practice</Link></header><section className="plan-wrap"><div className="kicker">PERSONAL STUDY PLAN</div><h1>Imtihoningizga mos reja.</h1><p className="lead">Reja tanlangan fanlar, real natijalar, maqsad ball va imtihongacha qolgan vaqtga qarab tuziladi.</p>{loading&&<div className="state-card">Ma’lumotlar yuklanmoqda…</div>} {!loading&&<form onSubmit={submit} className="form-card"><label>Imtihon sanasi<input type="date" value={date} min={new Date().toISOString().slice(0,10)} onChange={e=>setDate(e.target.value)} required /></label><label>Maqsad ball<select value={target} onChange={e=>setTarget(Number(e.target.value))}>{[60,70,80,90,95].map(x=><option key={x} value={x}>{x}</option>)}</select></label><label>Hozirgi daraja<select value={level} onChange={e=>setLevel(e.target.value)}><option>Boshlang‘ich</option><option>O‘rta</option><option>Yaxshi</option><option>Yuqori</option></select></label><label>Kunlik vaqt<select value={minutes} onChange={e=>setMinutes(Number(e.target.value))}><option value="30">30 daqiqa</option><option value="60">1 soat</option><option value="90">1.5 soat</option><option value="120">2 soat</option><option value="180">3 soat</option></select></label><fieldset><legend>Fanlar</legend><div className="subject-checks">{subjects.map((s:any)=><label key={s.id} className="check-row"><input type="checkbox" checked={selected.includes(s.id)} onChange={e=>setSelected(v=>e.target.checked?[...v,s.id]:v.filter(id=>id!==s.id))}/><span>{s.icon}</span>{s.name}</label>)}</div></fieldset>{error&&<div className="error-box">{error}</div>}<button className="primary" disabled={loading||!date||!selected.length}>{loading?'Yuklanmoqda…':'Rejani yaratish →'}</button></form>}{plan&&<div className="generated"><div><b>{plan.horizon||plan.days}</b><span>kunlik yo‘l xaritasi</span></div><div><b>{minutes} daq.</b><span>kunlik vaqt</span></div><div><b>{target}</b><span>maqsad ball</span></div><div className="weekly"><h2>Reja saqlandi</h2><p>Keyingi natijalaringiz zaif mavzular ustuvorligini o‘zgartiradi. Mocklar imtihon yaqinlashganda ko‘payadi.</p><Link href="/dashboard" className="secondary">Dashboardga o‘tish</Link></div></div>}</section></main>}
