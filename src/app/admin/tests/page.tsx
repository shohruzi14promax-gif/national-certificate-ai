import Link from 'next/link'; import {requireAdmin} from '@/lib/auth';
export default async function AdminTests(){const {supabase}=await requireAdmin();const {data}=await supabase.from('tests').select('id,title,mode,duration_minutes,question_count,subjects(name)').order('created_at',{ascending:false});return <main className="dash-shell"><header className="dash-nav"><Link href="/admin">← Admin</Link></header><section className="dash-wrap"><div className="kicker">TESTS</div><h1>Mock va practice testlar.</h1><div className="admin-list">{(data??[]).map((t:any)=><article className="panel" key={t.id}><h2>{t.title}</h2><p>{t.subjects?.name||'Umumiy'} · {t.mode} · {t.question_count} savol · {t.duration_minutes||'—'} daqiqa</p></article>)}</div></section></main>}
