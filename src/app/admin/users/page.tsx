import Link from 'next/link'; import {requireAdmin} from '@/lib/auth';
export default async function AdminUsers(){const {supabase}=await requireAdmin();const {data}=await supabase.from('profiles').select('id,name,username,preferred_language,current_level,created_at').order('created_at',{ascending:false}).limit(200);return <main className="dash-shell"><header className="dash-nav"><Link href="/admin">← Admin</Link></header><section className="dash-wrap"><div className="kicker">USERS</div><h1>Foydalanuvchilar.</h1><div className="admin-list">{(data??[]).map((u:any)=><article className="panel" key={u.id}><h2>{u.name||u.username||'Student'}</h2><p>{u.preferred_language} · {u.current_level||'Daraja kiritilmagan'} · {new Date(u.created_at).toLocaleDateString('uz-UZ')}</p></article>)}</div></section></main>}
