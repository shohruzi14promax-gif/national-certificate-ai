import {NextResponse} from 'next/server'; import {z} from 'zod'; import {requireUser} from '@/lib/auth';
const schema=z.object({conversationId:z.string().uuid().optional(),message:z.string().min(1).max(4000),subjectId:z.string().uuid().optional()});
export async function POST(req:Request){try{const {supabase,user}=await requireUser();const body=schema.parse(await req.json());let conversationId=body.conversationId;if(!conversationId){const {data,error}=await supabase.from('ai_conversations').insert({user_id:user.id,subject_id:body.subjectId??null,title:body.message.slice(0,60)}).select('id').single();if(error)throw error;conversationId=data.id;}const {error:me}=await supabase.from('ai_messages').insert({conversation_id:conversationId,role:'user',content:body.message});if(me)throw me;const {data:history}=await supabase.from('ai_messages').select('role,content').eq('conversation_id',conversationId).order('created_at').limit(20);const key=process.env.AI_PROVIDER_API_KEY;const base=process.env.AI_PROVIDER_BASE_URL;const model=process.env.AI_MODEL||'gpt-4o-mini';if(!key||!base)return NextResponse.json({conversationId,reply:'Tutor hozircha sozlanmoqda. AI_PROVIDER_API_KEY va AI_PROVIDER_BASE_URL server environment variable sifatida qo‘shilgach, shu suhbat ishlaydi.'});const response=await fetch(`${base.replace(/\/$/,'')}/chat/completions`,{method:'POST',headers:{'content-type':'application/json',authorization:`Bearer ${key}`},body:JSON.stringify({model,messages:[{role:'system',content:'You are a calm Uzbek National Certificate tutor. Explain concepts clearly, give hints before answers when useful, and never claim generated content is official exam material.'},...(history??[])]}),cache:'no-store'});if(!response.ok)throw new Error(`AI provider error ${response.status}`);const json=await response.json();const reply=String(json.choices?.[0]?.message?.content||'Javob olinmadi.');await supabase.from('ai_messages').insert({conversation_id:conversationId,role:'assistant',content:reply});return NextResponse.json({conversationId,reply});}catch(e:any){return NextResponse.json({error:e?.message||'Tutor xatosi'},{status:e?.message==='UNAUTHORIZED'?401:400})}}
